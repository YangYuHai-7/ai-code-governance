{
  "schemaVersion": 1,
  "riskId": "webhook-secret-missing",
  "variant": "repaired",
  "entrypoint": "project-owned-test",
  "expected": "Missing webhook secret is rejected and configured secret succeeds."
}
