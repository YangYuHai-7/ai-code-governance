# U03 单用户试点体验与复核记录

## 身份与范围

- Persona ID：U03
- 技术层级：前端初级
- 应用任务：带筛选和表单校验的工单看板
- Candidate tarball SHA-256：`9fbe94736ed2db7784f1be9160ecaec1a54fe415e10a278a14876d0ccc67580c`（本地 `shasum -a 256` 匹配）
- AICG toolVersion / capabilityTrack / evidenceLevel：`0.2.0` / `generic-unknown v3.0` / `unverified`
- 临时工作目录：`/tmp/aicg-pilot-20260910/U03`（macOS CLI 输出规范化为 `/private/tmp/...`）
- 声明：本记录是模拟 persona 试点，不是真实客户、真人认证或生产背书。

## 执行证据

- 安装命令与退出码：`npm install --save-dev /tmp/aicg-pilot-candidate-20260910/ai-code-governance-0.2.0.tgz`，exit `0`；安装 1 个包，审计 0 个漏洞。
- `doctor` / `assess` / `architecture` 结果摘要：`npm exec -- aicg doctor . --json`、`assess . --json`、`architecture . --json` 均 exit `0`。doctor 检测 Node `24.18.0`、macOS arm64、Codex，目录非 Git 仓库；assess 输出 `ambiguous-skeleton` / `brownfield`、低置信度 ambiguous lifecycle、`generic-unknown`；architecture 因无实质源码返回条件式 `generic-module-first` 方案。
- 显式选择：客户端 `codex`；生命周期 `greenfield`；治理深度 `standard`；交互语言 `en`；产物语言 `en`；调用方式 `project-local`；目标 OS `macos`。
- `init` 命令与退出码：`npm exec -- aicg init . --config .pilot/init-config.json --yes --no-assist`，exit `0`；生成/管理 20 个治理文件，其中包括 routed `software-design-and-verification` Skill 与 technical standards，初始化检查通过。
- 应用主用户故事：通过表单创建具有合法状态的工单，并按 all/open/in-progress/closed 筛选；筛选不会改变原工单集合。
- 应用文件树摘要：`index.html`；`src/app/main.js`；`src/app/server.js`；`src/modules/tickets/ticket-board.js`；`test/tickets.test.js`；`package.json`；`.agents/skills/standards/software-design-and-verification/SKILL.md`；`.ai-governance/*`；`AGENTS.md`；`docs/ai/*`；`.pilot/*`。`node_modules` 未列入摘要。
- 成功路径测试：`creates and filters tickets without mutating the source list`。
- 两条失败/边界测试：`rejects titles shorter than three trimmed characters`；`rejects unsupported ticket status values`。
- 应用测试命令与退出码：测试先行 RED：`npm test` exit `1`（3 failed）；实现后最终 `npm test` exit `0`（3 passed，0 failed，`duration_ms=51.907875`）。
- 手工运行证据：HTTP smoke 得到页面、app module、domain module `200`，不存在路径 `404`。
- `aicg check` 结果与 evidence 状态：`npm exec -- aicg check . --json` exit `0`、`ok=true`；`present=pass`、`reachable=pass`、`enforced=unverified`、`realClientVerified=unverified`；architecture active，semantic design 仅 `stated-only`。
- `aicg complete --verify` 结果：`npm exec -- aicg complete . --verify "npm run test" --json` exit `0`；project verification `passed`、exit `0`；`enforced=unverified`，`generation.status=manual-only`。
- 绕过、人工帮助和未完成项：未使用治理 bypass 或 manual override。Builder 完成配置、实现与测试；这仍是 persona simulation。没有真实 Codex 加载、浏览器交互测试或 acceptance-results；本段记录 Builder 阶段，后续独立 Review 见下文。

### Builder 体验观察（非独立 Review）

- standard 深度确实生成并路由了软件设计与验证 Skill，且模块放置与先测试后实现的工作方式一致。
- 原生 JavaScript 前端仍落入 `generic-unknown`，标准为通用设计规则，没有前端状态、表单、DOM 或可访问性专项内容。
- project-local `npm exec -- aicg` 在所有阶段稳定；check/complete 清楚区分结构可达与 enforcement、真实客户端加载。
- lifecycle 的自动评估与显式 greenfield 选择不一致，但显式配置使 init 可继续且未被静默覆盖。

## 用户评分（1–5）

以下是受控 persona simulation 评分，不是用户原话。

| 维度 | 分数 | 证据/模拟观察 |
| --- | ---: | --- |
| 安装和首次成功 | 5 | 安装与 project-local 命令均一次成功。 |
| 输出易懂 | 4 | 英文 JSON 对初级前端可解析，但字段较多。 |
| 下一步明确 | 4 | architecture 与生成入口给出模块落位和验证方向。 |
| 规则帮助应用实现 | 4 | 通用设计 Skill 与业务约束有帮助，缺少前端专项。 |
| 错误可自行恢复 | 4 | 测试反馈明确，静态模块路由问题可通过 smoke 定位。 |
| 治理成本与风险匹配 | 4 | standard 产物较多，但与学习型项目的验证目标基本匹配。 |
| 对可交付的信心 | 4 | 主流程和负向测试通过，尚无独立 UI/可访问性检查。 |

## 独立 Review

- Reviewer 角色：独立多角色复核组（架构、全栈、业务分析、测试、UX）；未参与 Builder A 的实现。
- 主流程是否可运行：原样复跑 `npm test`、`aicg check`、`complete --verify` 均 exit `0`；HTTP smoke 为页面、app module、ticket module `200`，未知路径 `404`。静态资源和 domain 函数可运行，但没有浏览器交互 smoke。
- 测试是否真正覆盖成功 + 两条失败路径：3/3 passed，成功用例同时验证创建、筛选和原集合长度不变，另有短标题和非法状态两条失败。测试仍只覆盖 domain 函数，没有表单、筛选控件、错误提示、刷新恢复或浏览器兼容性。
- 业务不变量与权限边界：三个显式约束均在 domain 模块中落实；`filterTickets` 还返回克隆对象，避免调用方改写源对象。作为“工单看板”，应用却只能创建和筛选，不能变更工单状态、编辑、删除或追踪历史；也没有用户、角色或可见性边界，必须限定为单人演示而非生产工单系统。
- 目录、模块和依赖方向：`src/app` 单向依赖 `src/modules/tickets`，职责基本清晰；固定静态路由避免路径穿越。单行 `index.html` 让 UI 维护、审查和差异定位困难，但未形成循环依赖。
- 安全、可靠性与恢复：标题通过 `textContent` 写入 DOM（`src/app/main.js:9-14`），未发现直接 DOM XSS；所有 tickets 仅存在页面内数组（`main.js:3`），刷新全部丢失。无 CSP/安全响应头、错误遥测、存储恢复或身份鉴权；远程部署前必须补齐或明确本地-only 边界。
- AICG 生成内容的准确性与噪音：standard 生成的 design/verification Skill 正确提示模块职责和聚焦测试，结构检查与 evidence 边界诚实；但 `generic-unknown` 只给通用规则，没有前端状态生命周期、持久化、DOM/a11y/e2e 或工单授权专项指导，也没有在源码出现后重新建议更匹配的 profile。
- P0 / P1 / P2：**P0：无。P1（可靠性/应用；AICG 回修：是）**：刷新丢失全部工单，无法作为生产看板。**P1（业务/权限/应用；AICG 回修：是）**：没有状态迁移与权限模型，若按生产工单系统交付会造成未授权查看/操作；若范围只是单人演示必须显式降级承诺。**P1（治理/AICG；AICG 回修：是）**：原生前端仍无专项 profile 与浏览器交付门禁。**P2（代码/测试/安全/UX；AICG 回修：是）**：无 DOM/e2e/a11y 测试、安全响应头和可恢复错误态，HTML 单行化增加维护成本。
- 问题归属：持久化、工单生命周期、权限和 UI 测试属于应用；原生前端识别、源码后复扫路由和生产验收提示属于 AICG；未做真人/真实浏览器与真实 Codex 加载属于证据边界。
- 建议修复或延期理由：当前可保留为学习型 demo，P1 关闭前不能称生产级。应用应增加持久化、显式状态迁移规则、身份/角色决策和浏览器测试；AICG 应在 `standard` 深度识别 browser-only state，并要求回答“刷新后数据去哪、谁能读写、关键交互是否被真实浏览器验证”。

## 证据台账

- 匿名 receipt：`/tmp/aicg-pilot-20260910/U03/.pilot/receipt.json`。
- `npm exec -- aicg evidence record . --config .pilot/receipt.json --yes --json`：exit `0`，`evidenceKind=simulated-persona`、`qualification=simulation-only`。
- `npm exec -- aicg evidence status . --json`：exit `0`；`claimState=unverified`、`receiptCount=1`、`simulated-persona=1`、`real-project=0`、`eligibleForHumanCertificationReview=0`、`certified=false`。
- 台账文件：`/tmp/aicg-pilot-20260910/U03/docs/ai/certification-evidence.json`。记录后重跑测试、`check`、`complete --verify` 均 exit `0`。
- 时长边界：未预置计时器，receipt 使用 `durationMs=0` 而不估算。

## 最终候选原地升级复测（2026-09-10）

- 最终候选绑定：源码 commit `e6b27d763601a3632bb1394d0ff0df2a94176f9b`；tarball SHA-256 `6161f1aa5334e597ce0c0d9f9800f827f60f8ae25678c9f0f2a694ebde242017`，本地复算匹配。
- 升级方式与结果：从原始试点复制时保留 `AGENTS.md`、`.agents/skills`、`.ai-governance/manifest.json`、`.ai-governance/config.json` 和 `docs/ai/context-map.yaml`；以 `lifecycle=existing`、`existingCodeStrategy=new-code-standard` 执行 `init --yes --force --config`，旧治理在原路径升级成功，未移动或备份治理目录，init exit `0`。未添加或从业务文本推断风险信号。
- 源码与应用测试：`src/` + `test/` 摘要为 `e03ee50a68409b3cd317e32f600dcbd0b2722745ce9db32e11f462a3d990b7d9`，与原试点完全一致；`npm test` exit `0`，`3/3` passed。
- 命令矩阵：最终包安装、init、`doctor --json`、`assess --json`、`architecture --json`、`check --json`、`complete --verify "npm run test" --json`、receipt 记录前后两次 `evidence status --json` 及 `evidence record --json` 的退出码均为 `0`。
- 治理证据：`check` 为 `present=pass`、`reachable=pass`、`enforced=unverified`、`realClientVerified=unverified`。`complete` 顶层 `ok=true` 且项目测试命令通过，但 `productionReadiness=blocked`、`reviewEligibility=not-eligible`；三条业务约束证据为 `3 declared / 0 covered`，运行界面验证仍为 `unverified`（`browser-ui`、`node-http`、`file-persistence`）。
- 风险证据：没有 owner-confirmed risk signal，因此 risk evidence 为 `not-applicable`（`0 required / 0 covered`）；这不是低风险或已验证声明。
- Receipt 边界：最终 receipt 为 `simulated-persona`、`outcome=blocked`、`qualification=simulation-only`；`eligibleForHumanCertificationReview=0`、`certified=false`、整体 `claimState=unverified`。
- 未关闭项：本轮没有重新执行旧应用缺陷探针；由于应用源码摘要未变，前文记录的刷新丢失工单以及缺少状态迁移/授权模型仍不能关闭。本复测没有真人、真实客户、生产使用或认证证据，不构成产品可上线或能力已认证的声明。
