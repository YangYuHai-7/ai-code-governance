# U02 单用户试点体验与复核记录

## 身份与范围

- Persona ID：U02
- 技术层级：会写少量 HTML/CSS
- 应用任务：响应式家庭记账前端
- Candidate tarball SHA-256：`9fbe94736ed2db7784f1be9160ecaec1a54fe415e10a278a14876d0ccc67580c`（本地 `shasum -a 256` 匹配）
- AICG toolVersion / capabilityTrack / evidenceLevel：`0.2.0` / `generic-unknown v3.0` / `unverified`
- 临时工作目录：`/tmp/aicg-pilot-20260910/U02`（macOS CLI 输出规范化为 `/private/tmp/...`）
- 声明：本记录是模拟 persona 试点，不是真实客户、真人认证或生产背书。

## 执行证据

- 安装命令与退出码：`npm install --save-dev /tmp/aicg-pilot-candidate-20260910/ai-code-governance-0.2.0.tgz`，exit `0`；安装 1 个包，审计 0 个漏洞。
- `doctor` / `assess` / `architecture` 结果摘要：运行 `npm exec -- aicg doctor . --json`、`npm exec -- aicg assess . --json`、`npm exec -- aicg architecture . --json`，三者 exit 均为 `0`。`doctor` 检测 Node `24.18.0`、macOS arm64、Git 可用但非 Git 仓库、Codex 已检测；`assess` 输出 `ambiguous-skeleton` / `brownfield`、低置信度 `ambiguous` 生命周期、`generic-unknown`；`architecture` 在无实质源码时给出条件式 `generic-module-first` 建议。
- 显式选择：客户端 `codex`；生命周期 `greenfield`；治理深度 `minimal`；交互语言 `zh-CN`；产物语言 `zh-CN`；调用方式 `project-local`；目标 OS `macos`。
- `init` 命令与退出码：`npm exec -- aicg init . --config .pilot/init-config.json --yes --no-assist`，exit `0`；生成/管理 15 个治理文件，初始化后检查通过。
- 应用主用户故事：在窄屏和宽屏页面中，通过可见标签表单添加收入/支出，实时查看收入、支出与余额；无效金额和未知类型被拒绝。
- 应用文件树摘要：`index.html`；`styles.css`；`src/app/main.js`；`src/app/server.js`；`src/modules/budget/budget.js`；`test/budget.test.js`；`package.json`；`.ai-governance/*`；`AGENTS.md`；`docs/ai/*`；`.pilot/*`。`node_modules` 未列入摘要。
- 成功路径测试：`calculates income, expense, and balance`，验证收入、支出和余额汇总。
- 两条失败/边界测试：`rejects zero or negative amounts`；`rejects unknown entry types`。
- 应用测试命令与退出码：测试先行 RED：`npm test` exit `1`（3 failed）；实现后最终 `npm test` exit `0`（3 passed，0 failed，`duration_ms=48.778666`）。
- 手工运行证据：HTTP smoke 得到页面、app module、domain module 均 `200`，不存在的静态路径 `404`。
- `aicg check` 结果与 evidence 状态：`npm exec -- aicg check . --json` exit `0`，`ok=true`；`present=pass`、`reachable=pass`、`enforced=unverified`、`realClientVerified=unverified`。缺少 acceptance-results 的警告被保留。
- `aicg complete --verify` 结果：`npm exec -- aicg complete . --verify "npm run test" --json` exit `0`；项目验证 `passed`、exit `0`；governance enforcement 仍为 `unverified`，generation 为 `manual-only`。
- 绕过、人工帮助和未完成项：未使用治理绕过或人工 override。Builder 准备配置、解释诊断并实现应用；模拟 persona 并未独立完成 JS 模块和自动化测试。没有浏览器视觉/辅助技术的真实环境检查，也没有真实 Codex 加载证据；本段记录 Builder 阶段，后续独立 Review 见下文。

### Builder 体验观察（非独立 Review）

- zh-CN 配置没有覆盖主要 CLI 诊断说明；对于只会少量 HTML/CSS 的用户，`brownfield`、manifest、enforcement 等字段仍需帮助解释。
- 生成的 `src/app` 与 `src/modules/<domain>` 放置规则直接帮助形成清晰目录，minimal 深度未额外生成标准 Skill。
- 原生 HTML/CSS/JS 前端仍映射为 `generic-unknown`，没有获得前端可访问性或响应式专项标准；本应用只能依靠显式 domain constraints 补足。
- project-local 调用从 init 到 complete 均可运行，结构与业务验证边界表达诚实。

## 用户评分（1–5）

以下是受控 persona simulation 评分，不是用户原话。

| 维度 | 分数 | 证据/模拟观察 |
| --- | ---: | --- |
| 安装和首次成功 | 4 | 安装一次成功，命令仍由 Builder 提供。 |
| 输出易懂 | 3 | JSON 稳定但 zh-CN 未覆盖核心诊断。 |
| 下一步明确 | 3 | lifecycle 提示存在，应用实现入口仍需人工解释。 |
| 规则帮助应用实现 | 4 | 模块目录和三个记账不变量均可直接落实。 |
| 错误可自行恢复 | 4 | 测试和 HTTP 失败清晰；AICG 术语是主要障碍。 |
| 治理成本与风险匹配 | 4 | minimal 深度对小型前端基本合适。 |
| 对可交付的信心 | 4 | 自动化和 HTTP smoke 通过，但尚无独立可访问性复核。 |

## 独立 Review

- Reviewer 角色：独立多角色复核组（架构、全栈、业务分析、测试、UX）；未参与 Builder A 的实现。
- 主流程是否可运行：原样复跑 `npm test`、`aicg check`、`complete --verify` 均 exit `0`；HTTP smoke 为页面、app module、domain module `200`，未知路径 `404`。未进行真实浏览器表单、窄屏布局或辅助技术自动化，因此 smoke 只证明静态资源可达。
- 测试是否真正覆盖成功 + 两条失败路径：3/3 passed，形式上覆盖汇总成功、零金额和未知类型；测试名声称“zero or negative”但断言只传 `0`（`test/budget.test.js:18-22`），未覆盖负数、三位小数、合法两位小数、DOM 提交、窄屏和刷新恢复。独立边界探针实际发现 `0.29`、`0.58` 被错误拒绝。
- 业务不变量与权限边界：类型白名单正确；金额两位小数检查使用二进制浮点严格相等（`src/modules/budget/budget.js:5-8`），会把合法货币值 `0.29`/`0.58` 判为非法。页面写“本月汇总”，但记录没有日期或月份，实际上只是“当前页面会话汇总”。纯前端无权限边界；若家庭数据需要多人或跨设备使用，该产品模型尚未定义。
- 目录、模块和依赖方向：UI 装配依赖 budget domain，模块方向清晰；静态服务路由采用固定 allowlist，没有路径穿越。同步读文件适合试点，生产流量下应改缓存或非阻塞交付。
- 安全、可靠性与恢复：用户备注用 `textContent` 渲染（`src/app/main.js:13-17`），未见 DOM XSS；但所有账目只存在内存数组（`main.js:3`），刷新即永久丢失。没有导出/备份、存储失败恢复、CSP 等安全响应头或隐私说明，家庭财务数据不能据此宣称生产可用。
- AICG 生成内容的准确性与噪音：结构和三条约束被记录，门禁对 `unverified` 的表述诚实；但原生 HTML/CSS/JS 被归入 `generic-unknown`，minimal 没有前端持久化、货币精度、可访问性、响应式验证或浏览器测试提示，导致最关键的业务错误和数据丢失未被治理流程发现。
- P0 / P1 / P2：**P0：无。P1（业务/代码/应用；AICG 回修：是）**：合法金额被浮点判断误拒，应使用整数分或十进制定点策略并补边界测试。**P1（可靠性/UX/应用；AICG 回修：是）**：刷新丢失全部账目，与可交付记账产品不符。**P1（治理/AICG；AICG 回修：是）**：缺少原生前端/货币领域的检查清单与复扫路由。**P2（测试/UX/安全；AICG 回修：是）**：“本月”语义不实、负数未测、无浏览器/窄屏/a11y 验证和基础安全响应头。
- 问题归属：金额算法、会话态、“本月”文案和测试覆盖属于应用；技术栈始终 `generic-unknown`、未提示货币定点/持久化/浏览器验证属于 AICG；没有真人可访问性测试属于证据边界。
- 建议修复或延期理由：虽无 P0，但 P1 未关闭前只能作为演示。应用先采用整数分模型、加入日期/月度筛选与持久化，补合法 `0.29`、负数、三位小数、刷新恢复和真实 DOM 测试；AICG 应识别原生浏览器应用并生成货币数据、持久化、a11y/响应式验收提示，在 `complete` 摘要中明确“只有 domain unit tests，没有 browser verification”。

## 证据台账

- 匿名 receipt：`/tmp/aicg-pilot-20260910/U02/.pilot/receipt.json`。
- `npm exec -- aicg evidence record . --config .pilot/receipt.json --yes --json`：exit `0`，`evidenceKind=simulated-persona`、`qualification=simulation-only`。
- `npm exec -- aicg evidence status . --json`：exit `0`；`claimState=unverified`、`receiptCount=1`、`simulated-persona=1`、`real-project=0`、`eligibleForHumanCertificationReview=0`、`certified=false`。
- 台账文件：`/tmp/aicg-pilot-20260910/U02/docs/ai/certification-evidence.json`。记录后重跑测试、`check` 和 `complete --verify` 均 exit `0`。
- 时长边界：未预置计时器，receipt 使用 `durationMs=0` 而不估算。

## 最终候选原地升级复测（2026-09-10）

- 最终候选绑定：源码 commit `e6b27d763601a3632bb1394d0ff0df2a94176f9b`；tarball SHA-256 `6161f1aa5334e597ce0c0d9f9800f827f60f8ae25678c9f0f2a694ebde242017`，本地复算匹配。
- 升级方式与结果：从原始试点复制时保留 `AGENTS.md`、`.ai-governance/manifest.json`、`.ai-governance/config.json` 和 `docs/ai/context-map.yaml`；以 `lifecycle=existing`、`existingCodeStrategy=new-code-standard` 执行 `init --yes --force --config`，旧治理在原路径升级成功，未移动或备份治理目录，init exit `0`。未添加或从业务文本推断风险信号。
- 源码与应用测试：`src/` + `test/` 摘要为 `07f51aed2cf55811c0f72a06180149d3451a93950ebba3a2da5880a3993a6e41`，与原试点完全一致；`npm test` exit `0`，`3/3` passed。
- 命令矩阵：最终包安装、init、`doctor --json`、`assess --json`、`architecture --json`、`check --json`、`complete --verify "npm run test" --json`、receipt 记录前后两次 `evidence status --json` 及 `evidence record --json` 的退出码均为 `0`。
- 治理证据：`check` 为 `present=pass`、`reachable=pass`、`enforced=unverified`、`realClientVerified=unverified`。`complete` 顶层 `ok=true` 且项目测试命令通过，但 `productionReadiness=blocked`、`reviewEligibility=not-eligible`；三条业务约束证据为 `3 declared / 0 covered`，运行界面验证仍为 `unverified`（`browser-ui`、`node-http`、`file-persistence`）。
- 风险证据：没有 owner-confirmed risk signal，因此 risk evidence 为 `not-applicable`（`0 required / 0 covered`）；这不是低风险或已验证声明。
- Receipt 边界：最终 receipt 为 `simulated-persona`、`outcome=blocked`、`qualification=simulation-only`；`eligibleForHumanCertificationReview=0`、`certified=false`、整体 `claimState=unverified`。
- 未关闭项：本轮没有重新执行旧应用缺陷探针；由于应用源码摘要未变，前文记录的合法金额误拒和刷新丢失账目仍不能关闭。本复测没有真人、真实客户、生产使用或认证证据，不构成产品可上线或能力已认证的声明。
